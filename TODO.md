# merge-game — TODO

## Спрайты — генерация через Replicate (recraft-v3)

**Концепция:** ювелирная/блестящая тема для Светланы.  
10 типов-цепочек, каждая — 10 уровней эволюции. Итого 100 PNG, 192×192px, прозрачный фон, единый стиль.

### Типы (предварительно, уточнить)
1. **Кристаллы** — осколок → кристалл → друза → геод → … → огромный кристалл
2. **Бриллианты** — крошка → кабошон → бриллиант → … → легендарный солитер
3. **Кольца** — простое → серебряное → золотое → с камнем → … → корона-кольцо
4. **Серёжки** — гвоздик → капля → подвеска → … → люстра
5. **Ожерелья** — нитка бус → чокер → колье → … → парюр
6. **Короны** — ободок → тиара → корона → … → императорская корона
7. **Зелья** — зелье блеска → … (вспомогательный тип, если нужен)
8–10. TBD — обсудить

### Задачи

- [ ] Финализировать список 10 типов и названия уровней внутри каждого
- [ ] Написать скрипт `scripts/gen-sprites.js` (Node.js + Replicate API)
  - Читает `.env` → `REPLICATE_API_TOKEN`
  - Принимает тип и уровень (или генерирует всё разом)
  - Отправляет запрос на `recraft-ai/recraft-v3`, 192×192, прозрачный фон
  - Сохраняет в `assets/<type>/level<N>.png`
- [ ] Составить промпты для каждого типа (общий стиль + per-level описание предмета)
- [ ] Прогнать генерацию — сначала 1 тип пробно, оценить качество
- [ ] Сгенерировать все 100 спрайтов
- [ ] Покрасить/докрутить неудачные вручную или перезапросить
- [ ] Нагенерить спрайты для заказчиков (customer panel) в том же стиле

## Код — в работе

- [x] ELEM_SCALE переведён на 192px базу (`/ 192`)
- [x] Drag корзинки movement-based: порог 20px от точки касания, без таймера
- [ ] Подключить новые 4 типа корзинок в `BASKET_CONFIGS` (вместо ring/book)
- [ ] Локализация: определять язык через `navigator.language`, ru → русский, иначе английский

## Геймплей

- [ ] Customer / order system — уже есть базовая реализация, доработать под новые типы
- [ ] Animation polish — particle burst on merge, arc travel when fulfilling orders

## Деплой

- [ ] Bump версии в `version.js` перед каждым релизом
